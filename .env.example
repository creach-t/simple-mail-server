# Configuration du serveur
PORT=4058
HTTPS_PORT=4059
NODE_ENV=production  # production ou development

# Chemin vers les certificats SSL (utilisé uniquement si les fichiers existent)
SSL_PATH=/etc/letsencrypt/live/your-domain

# CORS (séparez plusieurs origines par des virgules)
ALLOWED_ORIGINS=https://votre-site.com,http://localhost:3000

# Configuration des emails
TO_EMAIL=votre.email@example.com
FROM_EMAIL=contact@votredomaine.com
FROM_NAME=Votre Nom ou Entreprise
SIGNATURE=L'équipe de votre site

# Personnalisation des emails de confirmation
CONFIRMATION_SUBJECT=Confirmation de votre message
# Personnalisez ces champs si vous souhaitez un message différent de celui par défaut
# CONFIRMATION_TEXT=Texte de confirmation personnalisé
# CONFIRMATION_HTML=HTML de confirmation personnalisé

# Configuration SMTP (facultatif - utilisez ces variables si vous avez un service externe comme SendGrid)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=votre_cle_api_sendgrid